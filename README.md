# Movies project Read me file

Database Schema:

CREATE TABLE IF NOT EXISTS public.movies
(
id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 9223372036854775807 CACHE 1 ),
genres character varying(255) COLLATE pg_catalog."default" NOT NULL,
language character varying(255) COLLATE pg_catalog."default" NOT NULL,
overview character varying(1000) COLLATE pg_catalog."default" NOT NULL,
poster_url character varying(255) COLLATE pg_catalog."default" NOT NULL,
rating double precision NOT NULL,
release_date date NOT NULL,
runtime integer NOT NULL,
title character varying(255) COLLATE pg_catalog."default" NOT NULL,
CONSTRAINT movies_pkey PRIMARY KEY (id),
CONSTRAINT ukgovm2dombrdnujupo3o7hvtep UNIQUE (title)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.movies
OWNER to postgres;

Database table Movie is initially populated with 56 entries on start of application.


API key description:

API key system is implemented as a header parameter which will protect the endpoints from the unauthorized access.
Set your personal key in application.properties and use it for access to every endpoint defined in the project. (Default value 'mytoken' is set).
If you put invalid api key in a request header, appropriate error response will be returned with the message that api key is invalid.
Header Attribute name is 'api_key'



Endpoints in the API (use swagger UI on localhost:8080/swagger-ui/index.html):

URL: /movies/{id}
Required parameters: id, api_key

URL examples:
http://localhost:8080/movies/3
http://localhost:8080/movies/-1

Change token to something else to reproduce invalid api key error.

Will return a movie details for a given id, in the following form:
- Title
- Release Date
- Full Poster URL
- Overview
- Genres
- Average Rating
- Runtime
- Language

For a given id that doesn't exists in a Movie table, error message will be returned with the information that id for a Movie is not found.



URL: /movies/popular
Required parameters: api_key
Optional parameters: page (default one)

URL examples:
http://localhost:8080/movies/popular?page=1
http://localhost:8080/movies/popular?page=2
http://localhost:8080/movies/popular?page=3
http://localhost:8080/movies/popular?page=-1

Change token to something else to reproduce invalid api key error

E.g. If you enter a value 1 for a page, it will return a page of details of top 50 movies, or remaining movies if there are less then 50 (they will be sorted by rating), or empty page if there are no movies remaining.
If you enter a value 2 for a page, it will return a page of details for next top 50 movies, or remaining movies if there are less then 50, or empty page if there are no movies remaining.

Movie details form:
- Movie ID
- Title
- Release Date
- Poster URL
- Average Rating



URL: /movies/search
Required parameters: query, api_key
Optional parameters: genre, rating, sortBy (allowed values: rating, release_date, title; default value: rating)

URL examples:
http://localhost:8080/movies/search?query=inception&genre=action&rating=5&sortBy=rating
http://localhost:8080/movies/search?query=inception&sortBy=rating
http://localhost:8080/movies/search?query=in&rating=8&sortBy=release_date

Change token to something else to reproduce invalid api key error

Will return a list of movies which fulfill the filters for a title (query field), and optionally genre and rating.
For entered query, genre and rating, response will return movies whose title conatins text from query input, whose genre contains text from genre input, and whose rating is greater than or equal to rating from input.
If sortBy field is 'rating', movies will be sorted descending, from best to worst average rating.
If sortBy field is 'release date' movies will be sorted descending, from newest to oldest movie.
If sortBy field is 'title', movies will be sorted ascending, lexicografically (From A to Z).

Movie details form:
- Movie ID
- Title
- Release Date
- Poster URL
- Average Rating

















